{
  "compilerOptions": {
    "noImplicitOverride": true
  },
  "tasks": {
    "run": "deno run -A --watch server.ts",
    "test": "deno run -A --watch test.ts",
    "cacheLib": "deno cache --reload --lock ./lib/deps.ts",
    "cacheTst": "deno cache --reload --lock ./test/deps.ts"
  }
}
